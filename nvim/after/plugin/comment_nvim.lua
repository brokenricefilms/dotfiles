require('Comment').setup {
    padding = true,
    sticky = true,
    ignore = nil,

    toggler = {
        line = 'mm',
    },

    opleader = {
        line = 'mm',
        block = 'mb',
    },

    mappings = {
        basic = true,
        extra = true,
        extended = false,
    },

    pre_hook = nil,
    post_hook = nil,
}
