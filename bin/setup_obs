#!/usr/bin/env bash

if [ ! -d "~/repos/thuanowa/obs-studio" ]; then
  github_user_repo=~/repos/thuanowa/

  if [ ! -d "$github_user_repo" ]; then
    mkdir -p $github_user_repo
  fi

  cd $github_user_repo

  gh repo clone thuanowa/obs-studio
  ln -sf ~/repos/thuanowa/obs-studio/ ~/.config/
fi
