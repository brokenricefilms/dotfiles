#!/usr/bin/env bash
#
function fzf_tldr() {
  tldr --list | sed "s/'//g" | sed "s/,//g" | sed "s/ /\n/g" | sed "s/]//g" | sed "s/\[//g" | fzf --preview "tldr {1}"
}

if [ -z "$1" ]; then
  COMMAND=$(fzf_tldr)
  tldr $COMMAND
else
  tldr "$1"
fi
