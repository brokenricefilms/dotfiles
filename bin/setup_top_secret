#!/usr/bin/env bash

if [ ! -d "~/repos/thuanowa/ok" ]; then
  github_user_repo=~/repos/thuanowa/

  if [ ! -d "$github_user_repo" ]; then
    mkdir -p $github_user_repo
  fi

  cd $github_user_repo

  gh repo clone thuanowa/ok
fi
