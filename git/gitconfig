[alias]
a = "!git commit --all --amend --no-edit"
l = "!git log --oneline --decorate --all --graph -n 10"
i = "!curl -sL https://www.toptal.com/developers/gitignore/api/$@gi"
st = "!git status -sb"
d = "!git diff"
ok = "!git add --all; git commit -m '[👌Auto commit]'; git push"
yo = "!git diff; echo -n '👉 Commmit message: '; read commit_message; git add --all; git commit -m \"$commit_message\""
todo = "!git add --all; git commit -m 'chore: TODO list'; git push"
fak = "!git reset --hard HEAD; git clean -df"

[user]
name = Thuan Pham
email = thuanowa@gmail.com

[core]
editor = $EDITOR
whitespace = fix

[merge]
tool = vimdiff

[mergetool]
prompt = true

[mergetool "vimdiff"]
cmd = nvim -d $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[difftool]
prompt = false

[diff]
tool = vimdiff
colorMoved = zebra

[credential "https://github.com"]
helper = !/usr/bin/gh auth git-credential

[pull]
rebase = true

[fetch]
prune = true

[pager]
diff = delta
log = delta
reflog = delta
show = delta

[delta]
light = false
side-by-side = true
line-numbers = true

[apply]
whitespace = fix

[color]
ui = true
diff = true
status = true
branch = true
interactive = true
